[tool.poetry]
name = "helpers"
version = "0"
authors = []
packages = [
    { include = "examples" },
    { include = "django_project" },
]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
